# Generated by `npx cap add android` on every build. Committing a few hundred
# files of Gradle scaffolding would mean maintaining them by hand, and they are
# reproducible from capacitor.config.json in about a minute.
android/

# Assembled from the WebAssembly build; never a source.
www/
