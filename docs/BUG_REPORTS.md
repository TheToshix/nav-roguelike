# Багрепорты

Дефекты, найденные при разработке «Нави». Один закрыт как подтверждение корректного
поведения, остальные исправлены.

Каждый отчёт содержит шаги воспроизведения, причину, правку и тест, который не даст дефекту
вернуться. Поскольку подземелье порождается из зерна, шаги воспроизведения точны: одно и то же
зерно всегда даёт один и тот же этаж.

| ID | Кратко | Серьёзность | Статус |
|---|---|---|---|
| [NAV-001](#nav-001) | Монстры появляются вплотную к лестнице прибытия | Критическая | Исправлено |
| [NAV-002](#nav-002) | Пропасти отрезают часть этажа | Критическая | Исправлено |
| [NAV-003](#nav-003) | Поле зрения несимметрично | Значительная | Исправлено |
| [NAV-004](#nav-004) | Двери считаются непроходимыми при ремонте связности | Значительная | Исправлено |
| [NAV-005](#nav-005) | `line()` возвращает конечную точку дважды | Значительная | Исправлено |
| [NAV-006](#nav-006) | Индексы экипировки повисают после удаления предмета | Критическая | Исправлено |
| [NAV-007](#nav-007) | Вий на 4 этаже непроходим | Значительная (баланс) | Исправлено |
| [NAV-008](#nav-008) | Загрузка сохранения с рассогласованной котомкой | Незначительная | Не дефект |
| [NAV-009](#nav-009) | Труп остаётся в списке, если убит последним ударом партии | Значительная | Исправлено |
| [NAV-010](#nav-010) | Обереги не защищают от того же эффекта, брошенного издалека | Значительная | Исправлено |
| [NAV-011](#nav-011) | Игла Кощеева лежит не на его этаже | Критическая | Исправлено |
| [NAV-012](#nav-012) | Полная котомка делает Кощея неубиваемым | Критическая | Исправлено |
| [NAV-013](#nav-013) | Партия заканчивается победой за четыре этажа до конца | Критическая | Исправлено |
| [NAV-014](#nav-014) | Призыв ничем не ограничен: 188 тварей на этаже | Значительная | Исправлено |
| [NAV-015](#nav-015) | Четвёртый пояс есть в игре и нет на титульном экране | Значительная | Исправлено |
| [NAV-016](#nav-016) | WASD «не работает»: переключатель есть, а найти нельзя | Значительная | Исправлено |
| [NAV-017](#nav-017) | Горизонтальная раскладка на телефоне: полоса опыта разрезана | Значительная | Исправлено |
| [NAV-018](#nav-018) | Тест бега читает за концом списка монстров — падение под ASan | Значительная | Исправлено |
| [NAV-019](#nav-019) | Брошенный надетый оберег жизни оставляет запас здоровья завышенным | Значительная | Исправлено |
| [NAV-020](#nav-020) | Заморозка героя ничего не делала: Морозко «сковывает», а ноги идут | Значительная (баланс) | Исправлено |
| [NAV-021](#nav-021) | Обращение к освобождённой памяти в `monster_summon` после призыва | Критическая | Исправлено |

---

## NAV-001

**Монстры появляются вплотную к лестнице, по которой герой пришёл**

**Серьёзность:** критическая — приводит к смерти, на которую игрок не может повлиять.
**Найдено:** прогоном ботов, при разборе аномально коротких партий.

### Шаги воспроизведения

1. Собрать проект и запустить `./build/nav --demo 8`.
2. Посмотреть на строку с зерном `1592590340`.

**Фактически:** партия закончилась на 13 ходу с 24 очками здоровья. При разборе видно, что герой
появился в окружении трёх тварей и был убит раньше, чем успел что-либо сделать.

**Ожидалось:** этаж может быть опасным, но у игрока должна быть возможность сыграть.

### Причина

`Game::populate()` вызывается из `ensure_level()` **до** того, как `enter_level()` поставит героя
на лестницу. Функция выбора свободной клетки исключала только текущую позицию героя — а та в
момент заселения указывала ещё на предыдущий этаж. В результате ничто не мешало разместить
монстра прямо на клетке будущего прибытия или рядом с ней.

### Правка

`Game::random_free_spot()` получил параметры `avoid` и `min_distance`, а заселение монстров
теперь держит дистанцию не менее 7 клеток от входа:

```cpp
const Vec2 spot = random_free_spot(lvl, lvl.entrance, 7);
```

Если этаж настолько тесный, что подходящей клетки нет, возвращается первая свободная — генерация
не должна зависать из-за декоративного ограничения.

### Регрессионный тест

`Integration.NoMonsterStartsAdjacentToTheArrivalStaircase` — 40 зёрен × 6 этажей, проверка, что
ни один монстр не находится в радиусе одной клетки от героя в момент прибытия.

---

## NAV-002

**Пропасти отрезают часть этажа, и лестница вниз становится недостижимой**

**Серьёзность:** критическая — партия становится непроходимой.
**Найдено:** тестом `MapGen.EveryWalkableCellIsReachableFromTheEntrance`.

### Шаги воспроизведения

1. Запустить `./build/tests/nav_tests --gtest_filter=MapGen.EveryWalkableCell*`.
2. Тест падает: `seed 101, depth 6 produced an unreachable region`.

Или вручную: сгенерировать этаж на глубине 12 с `chasm_chance = 100` и проверить связность.

**Фактически:** часть проходимых клеток недостижима от входа.
**Ожидалось:** связность гарантирована на любом этаже.

### Причина

Порядок в `generate_level()`: связность обеспечивается, затем расставляются пропасти, затем
запускается ремонт связности. Ремонт пробивает туннель от отрезанной области к ближайшей
достижимой клетке функциями `carve_h`/`carve_v`, а те превращали в пол **только стены**:

```cpp
if (map.at({x, y}) == Tile::Wall) map.set({x, y}, Tile::Floor);
```

Если туннель пересекал пропасть, он в ней и заканчивался — то есть не соединял ничего. Ремонт
считал работу выполненной, а область оставалась отрезанной.

### Правка

Введён предикат «через что можно копать», и в него добавлены пропасти:

```cpp
bool diggable(Tile t) { return t == Tile::Wall || t == Tile::Chasm; }
```

### Регрессионный тест

`MapGen.ChasmsNeverStrandPartOfTheLevel` — 120 зёрен на глубине 12 с вероятностью пропастей 100%,
то есть в самых тяжёлых условиях, которые игра вообще может создать.

---

## NAV-003

**Поле зрения несимметрично: монстр видит героя, герой монстра — нет**

**Серьёзность:** значительная — читается как жульничество игры.
**Найдено:** тестом `Fov.IsSymmetricOnOpenTerrain`.

### Шаги воспроизведения

1. Построить карту 21×21 со случайными стенами (18%).
2. Посчитать обзор из центра, взять любую видимую клетку пола.
3. Посчитать обзор уже из неё и проверить, виден ли центр.

**Фактически:** для части клеток центр не виден.
**Ожидалось:** видимость между двумя клетками пола — симметричное отношение.

В игре это проявлялось так: Аспид со своего места имел линию до героя и плевался ядом, а на
экране героя эта клетка была тёмной, и источник урона был не виден.

### Причина

Классическое рекурсивное теневое литьё (формулировка Бергстрёма) не гарантирует симметрии.
Клетка освещается, если её задел клин видимости хотя бы краем, а границы клина при разных
направлениях обхода округляются по-разному.

### Правка

Алгоритм заменён на симметричную формулировку Альберта Форда. Ключевое отличие: клетка **пола**
освещается, только если её центр попал внутрь клина (`col >= row * start && col <= row * end`),
тогда как **стена** освещается при любом касании. Первое даёт симметрию, второе оставляет стены
сплошными.

Обход теперь идёт по четырём квадрантам вместо восьми октантов.

### Регрессионный тест

`Fov.IsSymmetricOnOpenTerrain`, плюс `Fov.LightPassesThroughADoorwayButNotAroundItsFrame` —
проверка, что при этом не потерялось затенение за косяком двери.

---

## NAV-004

**Закрытые двери считались непроходимыми, и ремонт связности рыл вокруг них лишние коридоры**

**Серьёзность:** значительная — портит форму этажей.
**Найдено:** при чтении сгенерированных карт.

### Шаги воспроизведения

1. Сгенерировать любой этаж с `door_chance = 55`.
2. Посмотреть на карту рядом с дверьми.

**Фактически:** почти у каждой двери есть параллельный коридор в обход.
**Ожидалось:** дверь — это проход, а не преграда; обходить её незачем.

### Причина

`blocks_move(Tile::Door)` возвращал `true`. Двери расставляются после первого прохода ремонта,
но перед вторым, и второй проход видел каждую комнату за дверью как отрезанную область и честно
пробивал к ней новый туннель.

### Правка

Закрытая дверь считается **проходимой**: шаг в неё открывает её и тратит ход. Непроходимы теперь
только стены и пропасти:

```cpp
inline bool blocks_move(Tile t) { return t == Tile::Wall || t == Tile::Chasm; }
inline bool blocks_sight(Tile t) { return t == Tile::Wall || t == Tile::Door; }
```

Дверь по-прежнему блокирует обзор — иначе она перестала бы быть дверью.

### Регрессионный тест

`MapBasics.ClosedDoorsBlockSightButNotMovement`,
`Movement.WalkingIntoAClosedDoorOpensIt` (первый шаг открывает дверь и тратит ход, но не двигает
героя; второй шаг проходит),
`Ai.MonstersPushDoorsOpenOnTheirWayThrough`.

---

## NAV-005

**`line()` возвращает конечную точку дважды**

**Серьёзность:** значительная — искажает урон заклятий по линии.
**Найдено:** тестом `Line.HandlesZeroLengthAndCardinalCases`.

### Шаги воспроизведения

```cpp
nav::line({0, 0}, {0, 3});   // ожидается 3 клетки
nav::line({4, 4}, {4, 4});   // ожидается пустой результат
```

**Фактически:** первый вызов возвращает 4 клетки, последняя дублируется; второй возвращает одну
клетку вместо пустого результата.

**Ожидалось:** источник исключён, цель включена ровно один раз.

### Причина

Проверка на достижение цели стояла в начале итерации и **добавляла** точку, хотя предыдущая
итерация её уже добавила:

```cpp
if (x0 == to.x && y0 == to.y) { out.push_back(to); break; }
```

### Последствия

Заклятье «Молния» бьёт всех на линии до цели. Из-за дубликата цель получала урон дважды, то есть
молния наносила по конечной цели вдвое больше заявленного.

### Правка

Цикл переписан так, что проверка на достижение цели только прерывает обход:

```cpp
for (int guard = 0; guard <= limit; ++guard) {
    if (x == to.x && y == to.y) break;
    // шаг Брезенхэма
    out.push_back({x, y});
}
```

### Регрессионный тест

`Line.HandlesZeroLengthAndCardinalCases`, `Line.EndsAtTheTargetAndExcludesTheOrigin`,
`Line.EveryStepIsAdjacentToTheLast`.

---

## NAV-006

**Индексы надетого снаряжения указывают не на тот предмет после удаления вещи из середины котомки**

**Серьёзность:** критическая — порча состояния, приводящая к неверным характеристикам и падению
загрузки.
**Найдено:** проектированием `Inventory` и подтверждено
`Integration.InvariantsHoldThroughoutLongRandomisedRuns`.

### Шаги воспроизведения

1. Иметь в котомке: `[0] зелье, [1] меч (надет), [2] кольчуга (надета)`.
2. Выпить зелье — оно удаляется из позиции 0.

**Фактически:** `weapon` остаётся равным 1, `armor` — 2. После сдвига в позиции 1 лежит уже
кольчуга, а позиции 2 не существует. Герой «носит» кольчугу как оружие, а показатель защиты
читает мусор за границей вектора.

**Ожидалось:** индексы сдвигаются вместе с содержимым.

### Причина

Экипировка хранится индексами в `std::vector<Item>`, а `erase` из середины сдвигает всё, что
правее. Классическая ошибка индексной ссылки на изменяемый контейнер.

### Правка

Удаление стало единственной точкой изменения, и она чинит индексы:

```cpp
items.erase(items.begin() + index);
auto fix = [index](int& slot) {
    if (slot == index) slot = -1;      // сняли то, что было надето
    else if (slot > index) --slot;     // сдвиг
};
fix(weapon); fix(armor); fix(amulet);
```

### Регрессионный тест

`Inventory.RemovingAnItemShiftsTheEquipmentIndicesDown`,
`Inventory.RemovingTheEquippedItemClearsItsSlot`,
`Inventory.RemovingALaterItemLeavesEarlierSlotsAlone`,
`Gear.DroppingSomethingWornTakesItOffFirst`,
плюс постоянная проверка в инвариантах: индекс экипировки обязан указывать на существующий
предмет **нужного вида**.

---

## NAV-007

**Вий на четвёртом этаже непроходим для героя подходящего уровня**

**Серьёзность:** значительная (баланс) — стена, за которую не проходит ни один прогон.
**Найдено:** прогоном ботов.

### Шаги воспроизведения

1. Запустить `./build/nav --demo 12`.
2. Посмотреть на распределение достигнутых глубин.

**Фактически:** все двенадцать партий заканчиваются на глубине 3–4. Ни один прогон не проходит
дальше четвёртого этажа.

**Ожидалось:** босс — препятствие, а не потолок. Часть прогонов должна проходить дальше.

### Разбор

Расчёт для Витязя 4 уровня против Вия в первой редакции:

| | Герой | Вий |
|---|---|---|
| Здоровье | 55 | 80 |
| Удар | 14 | 16 |
| Защита | 5 | 5 |
| Урон за удар (средний) | ≈ 10,5 | ≈ 13 |
| Ударов до победы | 8 | **4** |

Герою нужно восемь попаданий, Вию — четыре. При равной скорости бой проигран арифметически,
и никакое количество зелий это не спасает.

### Правка

| Босс | Было (hp/удар/защита) | Стало |
|---|---|---|
| Вий | 80 / 16 / 5 | **70 / 12 / 4** |
| Баба-Яга | 140 / 20 / 7 | **130 / 17 / 6** |
| Кощей | 260 / 28 / 10 | **240 / 24 / 9** |

Дополнительно естественное восстановление здоровья ускорено с 1 единицы за 18 ходов до 1 за 12:
до правки затяжной бой был невыгоден в принципе.

После правки: герою нужно 6 попаданий, Вию — 6. Бой стал напряжённым, но выигрываемым с зельями.

### Проверка

Прогон ботов после правки стабильно доходит до 4–6 этажа, отдельные прогоны проходят Вия:

```
seed=1592590338 turns=1010 depth=5 deepest=5 lvl=5 kills=42 score=1413
seed=1592590344 turns=1504 depth=6 deepest=6 lvl=5 kills=43 score=1562
```

### Примечание

Автотеста на баланс нет — это оценочная величина. Регрессия отслеживается прогоном ботов в CI:
резкое падение средней глубины будет заметно в логе задачи.

---

## NAV-008

**Загрузка отклоняет сохранение, где котомка пуста, а индексы экипировки заполнены**

**Серьёзность:** незначительная.
**Статус:** не дефект — поведение корректное.
**Найдено:** при написании теста `Save.PreservesWhatHasBeenIdentified`.

### Что произошло

Тест очищал котомку напрямую (`inv.items.clear()`), не сбрасывая `weapon` и `armor`. Сохранение
записалось с индексами, указывающими в пустой вектор. `load()` отказался его принять, и тест упал.

### Разбор

Отказ правильный. Проверка в `Game::load()`:

```cpp
const int slots = static_cast<int>(g.hero_.inv.items.size());
for (int* slot : {&g.hero_.inv.weapon, &g.hero_.inv.armor, &g.hero_.inv.amulet})
    if (*slot < -1 || *slot >= slots) return false;
```

делает ровно то, для чего написана: не даёт загрузить состояние, которое немедленно приведёт к
обращению за границу вектора. Дефект был в тесте, а не в движке.

### Что сделано

Тест исправлен: он теперь сбрасывает индексы вместе с очисткой котомки. Отчёт оставлен в
документе как пример полезного отказа — валидация загрузки поймала рассогласованное состояние
раньше, чем оно успело навредить.


---

## NAV-009

**Убитый последним ударом монстр остаётся в списке живых существ**

**Серьёзность:** значительная — нарушает инвариант, на который опираются оба фронтенда.
**Найдено:** тестом `Combat.BreakingTheNeedleMakesKoscheiMortal`, написанным под новую механику Кощея.

### Шаги воспроизведения

1. Сломать иглу, чтобы Кощей стал смертен.
2. Убить Кощея — победным, то есть последним ударом партии.
3. Посмотреть на `game.monsters()`.

**Фактически:** список не пуст, в нём лежит запись с `alive == false` и нулевым здоровьем.
**Ожидалось:** мёртвых в списке не бывает — это утверждается в инвариантах и на это
рассчитывают оба фронтенда, когда рисуют монстров и считают соседей.

### Причина

Уборка трупов (`reap_dead()`) вызывалась только внутри планировщика ходов, а тот выходит
сразу, как только партия закончилась:

```cpp
for (int guard = 0; guard < 512; ++guard) {
    if (state_ != RunState::Playing) return;   // <- выход до уборки
    ...
    reap_dead();
}
```

То есть любой удар, которым партия заканчивается — победный по Кощею или смертельный для
героя — оставлял тело в списке. Раньше это не проявлялось: единственная концовка была
«герой погиб», а погибший герой монстром не является.

### Правка

Уборка вынесена в `perform()` и вызывается по обе стороны от планировщика:

```cpp
reap_dead();                 // ход героя мог оставить труп
recompute_fov();
advance_until_hero_turn();
reap_dead();                 // и ход тварей тоже
recompute_fov();
```

### Регрессионный тест

`Combat.BreakingTheNeedleMakesKoscheiMortal`, `Koschei.BreakingTheNeedleIsWhatEndsHim`,
плюс постоянная проверка в инвариантах: в списке монстров не может быть мёртвых.

### Примечание

Этот дефект существовал с самого начала, но проявился только когда у игры появился второй
способ закончиться. Механика Кощея не создала баг — она открыла путь к нему, а тест на
механику нашла его в тот же час.

---

## NAV-010

**Обереги не защищают от того же эффекта, брошенного издалека**

**Серьёзность:** значительная — вещь не делает того, что написано на ней.
**Найдено:** тестом `Phases.TheWardingCircleIsTheAnswerToMara`, который писался не про
обереги, а про Мару.

### Шаги воспроизведения

1. Надеть Обережный круг (рогатина, сорочка-неуязвимка, наузы) — он обещает, что
   «ни морок, ни слепота не пристают».
2. Спуститься на третий этаж, к Маре. Она бьёт мороком на расстоянии.
3. Подождать шесть десятков ходов, не подходя вплотную.

**Ожидаемо:** морок не берёт.
**Фактически:** герой оглушён мороком примерно четверть времени.

### Разбор

У монстров два способа наложить свой эффект: удар в упор (`monster_attacks_hero`) и
бросок на расстоянии (`monster_ranged`). Проверка на иммунитет была написана только в
первом:

```cpp
// monster_attacks_hero
if (sp.on_hit_chance > 0 && rng_.chance(sp.on_hit_chance)) {
    const bool immune = /* ... четыре условия ... */;
    if (!immune) hero_.a.add_effect(sp.on_hit, sp.on_hit_turns, 2);
}

// monster_ranged — та же строка, но без вопроса
if (sp.on_hit_chance > 0 && rng_.chance(sp.on_hit_chance / 2))
    hero_.a.add_effect(sp.on_hit, sp.on_hit_turns, 2);
```

Дефект старше новых наборов: старый «Оберег от яда» точно так же не защищал от яда,
плюнутого аспидом с другого конца комнаты. Полгода никто этого не замечал, потому что
проверить оберег глазами почти невозможно — яд и так берёт не всегда.

### Правка

Вопрос «пристаёт ли к герою этот эффект» вынесен в одно место и задаётся обоими путями:

```cpp
bool Game::hero_resists(Effect e) const { /* амулет, наузы, набор */ }

// оба вызова теперь одинаковы
if (sp.on_hit_chance > 0 && rng_.chance(...) && !hero_resists(sp.on_hit))
    hero_.a.add_effect(sp.on_hit, sp.on_hit_turns, 2);
```

**Вывод на будущее.** Дублирование правила — это дублирование и его исключений. Второй
экземпляр отстал молча, и заметить это можно было только тестом, который проверяет
обещание вещи, а не путь, которым эффект пришёл.

---

## NAV-011

**Игла Кощеева лежит не на его этаже**

**Серьёзность:** критическая — Кощея нельзя убить, игра непроходима.
**Найдено:** обзорным прогоном (`nav --sweep`) в первый же запуск.

### Шаги воспроизведения

1. Дойти до двенадцатого этажа, к Кощею.
2. Сбить его до нуля. В журнале: «Смерть его — на конце иглы. Игла — на этом этаже.»
3. Обойти этаж целиком.

**Ожидаемо:** игла лежит где-то здесь.
**Фактически:** иглы нет. Она на шестнадцатом этаже, четырьмя пролётами ниже.

### Разбор

Игла ставилась так:

```cpp
if (depth == kMaxDepth) { /* положить иглу */ }
```

Когда это писалось, Кощей был последним боссом и `kMaxDepth` равнялось 12 — строка была
верной. Потом появился четвёртый пояс, `kMaxDepth` стало 16, и игла уехала вместе с ним.

Хуже всего то, что **тест на иглу продолжал проходить**:

```cpp
Game g = descend_to(kMaxDepth, seed);   // тоже 16
EXPECT_EQ(needles, 1);                  // игла там есть — на дне
```

Тест закрепил не правило, а число, и переехал вместе с числом. Формально зелёный, по сути
он проверял «игла лежит на последнем этаже», а нужно было «игла лежит там, где Кощей».

### Правка

Добавлена обратная к `boss_for_depth` функция, и место спрашивается по имени:

```cpp
int boss_depth(const char* species_key);   // «где стоит вот этот»

if (depth == boss_depth("koschei")) { /* положить иглу */ }
```

Тест переписан так же — от `boss_depth("koschei")`, а не от `kMaxDepth`.

**Вывод на будущее.** Тест, который берёт ту же константу, что и код, проверяет, что они
равны сами себе. Проверять надо правило: не «на этаже 16», а «на этаже, где этот босс».

---

## NAV-012

**Полная котомка делает Кощея неубиваемым**

**Серьёзность:** критическая — игрок навсегда заперт перед концовкой, и на экране нет
ничего, что объяснило бы почему.
**Найдено:** обзорным прогоном, после правки NAV-011: игла лежала под ногами, а бот
продолжал бить Кощея до собственной смерти.

### Шаги воспроизведения

1. Набрать двадцать предметов — котомка полна.
2. Спуститься к Кощею, найти иглу, встать на неё.
3. Нажать «подобрать».

**Ожидаемо:** игла берётся; Кощея можно убить.
**Фактически:** «Котомка полна». Игла остаётся на полу, Кощей встаёт снова и снова.
Единственное сообщение об этом — одна строка в журнале посреди боя, где её никто не читает.

### Разбор

Игла была обычным предметом и проходила через общую проверку вместимости:

```cpp
if (!hero_.inv.add(it)) {
    message(Text{"Котомка полна.", "Your pack is full."}, Severity::Bad);
    return false;
}
```

Для меча это правильно. Для предмета, без которого игра не заканчивается, — нет: правило
инвентаря молча закрывает единственный выход.

### Правка

Игла больше не попадает в котомку вовсе. Поднять её — значит сломать:

```cpp
if (it.kind == ItemKind::Needle) {
    floor.erase(floor.begin() + index);
    needle_broken_ = true;
    message(Text{"Ты поднимаешь иглу и ломаешь её. ...", "..."}, Severity::Critical);
    ...
}
```

Носить её незачем, лишний ход и лишняя ячейка ничего не добавляли, а ловушка исчезла целиком.
Тест `Koschei.AFullPackCannotLockThePlayerOutOfHisDeath` набивает котомку доверху и требует,
чтобы игла всё равно сломалась.

**Вывод на будущее.** Ограничение, разумное для всего остального, становится дефектом,
когда через него проходит единственный путь к концовке. Такие предметы стоит выносить из
общих правил, а не расширять правила.

---

## NAV-013

**Партия заканчивается победой за четыре этажа до конца**

**Серьёзность:** критическая — четверть игры недостижима.
**Найдено:** тем же обзорным прогоном: после правок NAV-011 и NAV-012 прогон убивал Кощея
и немедленно объявлялся победой, не дойдя до Пекла.

### Разбор

Условие победы было написано именем:

```cpp
if (std::strcmp(beasts[si].key, "koschei") == 0) {
    state_ = RunState::Ascended;
    message(Text{"... Навь отпускает тебя.", "... Nav lets you go."}, Severity::Critical);
}
```

Ровно та же ошибка, что и в NAV-011, только с другой стороны: «последний» было записано как
имя вместо вопроса. Пока Кощей был последним, всё сходилось.

### Правка

```cpp
const char* last = boss_for_depth(kMaxDepth);
if (last && std::strcmp(beasts[si].key, last) == 0) { /* победа */ }
```

Кощею оставлена своя строка о том, что игла сломана и он рассыпается прахом, — но партия
на этом не кончается. Тест `Koschei.TheEndingBelongsToTheBottomFloorsGuardian` требует, чтобы
концовка принадлежала тому, кто стоит на `kMaxDepth`, и отдельно проверяет, что это уже не
Кощей.

**Вывод на будущее.** Три дефекта подряд — одна причина: то, что относится к «последнему
боссу», записывалось константой вместо вопроса. После этих правок в движке не осталось ни
одного места, где босс назван по имени ради его положения.

---

## NAV-014

**Призыв ничем не ограничен: 188 тварей на этаже**

**Серьёзность:** значительная — бой превращается в другую игру, и этаж начинает тормозить.
**Найдено:** обзорным прогоном, который завис на двенадцатом этаже; отладочный вывод показал
188 живых монстров.

### Шаги воспроизведения

1. Дойти до Кощея и довести его до третьей фазы, где он зовёт навь из стен.
2. Не убивать его долго — например, драться в дверном проёме.
3. Посмотреть на этаж.

**Ожидаемо:** давление растёт, но остаётся боем.
**Фактически:** монстры прибывают быстрее, чем гибнут. За несколько тысяч ходов их
становится под две сотни, проходы забиты, до Кощея не дойти, кадр считается заметно дольше.

### Разбор

`monster_summon` ограничивал только частоту — перезарядкой в 6–12 ходов, — но не количество.
Частота без потолка это не ограничение: длинный бой всегда упирается в равновесие между
скоростью призыва и скоростью убийства, и если первая выше, этаж заполняется.

### Правка

Потолок по числу живых тварей рядом с призывателем, а не по всему этажу:

```cpp
constexpr int kBroodRadius = 8;
constexpr int kBroodLimit = 8;
int nearby = /* живые монстры в радиусе, кроме самого призывателя */;
if (nearby >= kBroodLimit) { m.summon_cooldown = 4; return; }
```

Так давление механики сохраняется — расчистил вокруг, и он зовёт снова, — а потоп исчезает.

**Вывод на будущее.** Ограничивать частоту недостаточно там, где эффект накапливается.
Накопительным механикам нужен потолок по запасу, а не только по расходу.

---

## NAV-015

**Четвёртый пояс существует в игре и не существует на титульном экране**

**Серьёзность:** значительная — игра рассказывает о себе неправду, и именно там, где игрок
решает, стоит ли начинать.
**Найдено:** игроком. «Обнови данные, где три пояса три стража, добавив Пекло».

### Шаги воспроизведения

1. Открыть титульный экран веб-версии.
2. Прочитать подпись над списком поясов и сам список.

**Ожидаемо:** четыре пояса — Погост, Чернотопь, Кощеево царство, Пекло — и восемь стражей.
**Фактически:** «Три пояса, три стража» и список из трёх. Пекло, четыре его этажа, Змей
Горыныч и Полоз в игре есть, но человек, не начавший партию, об этом не узнает.

### Разбор

```cpp
for (nav::Zone z : {nav::Zone::Pogost, nav::Zone::Chernotop, nav::Zone::Koshchei}) {
```

Список поясов был выписан руками во фронтенде — второй экземпляр знания, которое уже есть в
движке. Когда пояс добавили, обновили таблицу зон, генератор, бестиарий, палитры и музыку, а
эту строчку — нет, потому что ничто о ней не напоминало. Ровно тот же класс дефекта, что
NAV-011: два списка одного и того же, и разошлись они молча.

Подпись «Три пояса, три стража» — то же самое, только в виде текста.

### Правка

Список берётся из движка, и там же появилась функция, у которой этот список — единственная
обязанность:

```cpp
const std::vector<Zone>& descending_belts();
int belt_last_depth(Zone zone);
```

Фронтенд обходит её и заодно показывает для каждого пояса его этажи и стража, а подпись
считается по факту.

### Тест

Свойство сформулировано с другого конца — не «в списке четыре элемента», а «каждый этаж игры
принадлежит поясу, который список называет»:

```cpp
for (int depth = 1; depth <= kMaxDepth; ++depth)
    EXPECT_NE(std::find(belts.begin(), belts.end(), zone_for_depth(depth)), belts.end());
```

Пятый пояс, если он появится, сломает этот тест в ту же секунду, когда появится его первый
этаж, а не через релиз.

### Чему научило

Список чего угодно во фронтенде — это копия. Копия отстанет. Единственный способ, которым
этого можно избежать, — чтобы копии не было: фронтенд должен спрашивать, а не помнить.

---

## NAV-016

**WASD «не работает»: переключатель есть, а найти его нельзя**

**Серьёзность:** значительная — возможность есть в коде, но для игрока её нет.
**Найдено:** игроком. «У меня не работает wasd, и нет внутриигровых настроек, чтобы это
переключить».

### Шаги воспроизведения

1. Открыть игру, начать партию.
2. Захотеть играть на WASD. Нажать `w`.

**Ожидаемо:** либо герой идёт вверх, либо игра говорит, где это включается.
**Фактически:** ничего не происходит. В верхней панели есть кнопка с надписью `HJKL` — но это
название текущей схемы, а не того, что кнопка сделает. Игрок читает её как заголовок, а не
как переключатель, и ищет настройки, которых нет.

### Разбор

Механика была сделана и проверена тестами: обе схемы разбираются из одной таблицы, справка
из неё же генерируется, коллизий нет. Не было сделано ровно одного — места, где игрок мог бы
про это узнать. Кнопка, подписанная текущим состоянием, — классическая ловушка: `HJKL`
одинаково читается и как «сейчас HJKL», и как «включить HJKL», и первое прочтение
естественнее.

Тесты этого поймать не могли и не должны были: они проверяют, что схема работает, а дефект
был в том, что до неё нельзя дотянуться.

### Правка

Экран настроек — на титульном экране и в игре, — где каждая настройка это подписанный ряд
вариантов с выделенным текущим: управление, вид карты, графика, музыка, язык. Кнопка в
верхней панели теперь называется «Настройки»: она говорит, что сделает, а не как называется
то, что уже есть.

### Чему научило

«Реализовано и покрыто тестами» и «доступно игроку» — разные утверждения, и второе не
следует из первого. Кнопку стоит подписывать действием, а не состоянием.

---

## NAV-017

**Горизонтальная раскладка на телефоне: полоса опыта разрезана кнопками**

**Серьёзность:** значительная — в горизонтальной ориентации играть нельзя.
**Найдено:** игроком. «Уберите наложение текста друг на друга и на клавиши управления».

### Шаги воспроизведения

1. Открыть игру на телефоне и повернуть его горизонтально (844×390).
2. Посмотреть на панель справа и на нижний ряд кнопок.

**Ожидаемо:** всё читается и ничто ни на что не наезжает.
**Фактически:** карта сжата в правый угол, панель обрезана посередине строки, полоса опыта
разрезана пополам панелью действий, крестовик занимает половину экрана.

### Разбор

Раскладка под телефон была написана и проверена только вертикально: `max-height: 38vh` у
боковой панели и крестовик высотой 142 px нормально уживаются при высоте экрана 844 и не
уживаются при 390. Одна медиа-выборка `max-width: 860px` описывала «телефон» вообще, хотя
телефон в двух ориентациях — это две разные задачи компоновки.

### Правка

Отдельная раскладка для горизонтали: карта во всю высоту, панель сбоку как на десктопе,
крестовик и действия — плавающими блоками в тех углах, где и так лежат большие пальцы.
Вертикальная тоже переписана: крестовик и действия стали двумя явными колонками, а не одной
строкой с переносом, из-за которого «Колдовать» ложилось на «Жертву».

### Тест

Проверка выполняется в браузере, а не в GoogleTest: страница открывается в трёх размерах
телефона и попарно сравниваются прямоугольники всех кнопок, полос и таблиц. Любое пересечение
больше двух пикселей — падение. Так дефект стал воспроизводимым без телефона в руках.

### Чему научило

«Мобильная вёрстка» — не одна проверка, а минимум две. И то, что глазами проверялось только
в одной ориентации, во второй почти наверняка сломано.

---

## NAV-018

**Тест бега читает за концом списка монстров: падение под AddressSanitizer**

**Серьёзность:** значительная — задача `sanitizers` в CI падает целиком. В обычной сборке тест
проходит по счастливой случайности, читая освободившуюся ёмкость вектора.
**Найдено:** прогоном набора тестов под ASan + UBSan — той же сборкой, что гоняет CI, и
которая на Этапе 0 запускается первой.

### Шаги воспроизведения

1. Собрать с санитайзерами, как в `.github/workflows/ci.yml`:

   ```
   cmake -S . -B build-san -DCMAKE_BUILD_TYPE=Debug \
     -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-sanitize-recover=all"
   cmake --build build-san
   ctest --test-dir build-san
   ```

2. `Travel.RunningIntoAFoeIsJustAnAttack` завершается аварийно:
   `AddressSanitizer: container-overflow ... test_travel.cpp:178`.

**Фактически:** abort в санитайзерной сборке; в релизной сборке тест проходит.
**Ожидалось:** тест проходит в обеих.

### Причина

Тест ставит анчутку (6 очков здоровья) вплотную к герою и делает `Run` в её сторону. При
видимом враге `act_run` вырождается в один `Move`, то есть в удар. Урон Витязя с топором —
9–15, всегда больше шести, так что анчутка гибнет с первого удара; `reap_dead()` убирает
труп, и `game.monsters()` становится пустым. Следующая строка

```cpp
EXPECT_LT(c.game.monsters()[0].a.hp, hp) << "the bump did no damage";
```

обращается к `[0]` пустого вектора. Память под этот элемент ещё принадлежит вектору (это его
ёмкость, не размер) и хранит прежнее значение здоровья, меньшее шести, — поэтому в релизе
`EXPECT_LT` проходит. Аннотации контейнеров в ASan видят чтение между `size()` и `capacity()`
и валят процесс.

### Правка

Врагу в этом тесте поднимают здоровье до 60, чтобы он пережил один удар, и перед чтением
добавляется `ASSERT_EQ(c.game.monsters().size(), 1u)`. Смысл теста — «шаг в тварь это удар,
а не проход насквозь» — сохранён, но проверяется он теперь на живом монстре.

### Регрессионный тест

Сам исправленный `Travel.RunningIntoAFoeIsJustAnAttack`: `ASSERT` на размер списка делает
предположение «монстр ещё жив» явным, и если удар снова начнёт убивать цель за один раз,
тест упадёт с внятным сообщением, а не с обращением за границу.

### Чему научило

Любое `monsters()[k]` после боя должно сначала убедиться, что монстр жив. И прогон под
санитайзерами — часть Этапа 0, а не только зелёная галочка в CI: этот дефект жил в наборе
тестов и был невидим ровно до неё.

---

## NAV-019

**Брошенный надетый «Оберег жизни» оставляет максимум здоровья завышенным**

**Серьёзность:** значительная — производная характеристика остаётся неверной до конца партии
(или до следующего надевания или снятия чего угодно, что её пересчитает).
**Найдено:** аудитом кода — просмотром всех путей, которыми предмет покидает котомку.

### Шаги воспроизведения

1. Терминальная сборка. Начать партию Витязем.
2. Поднять и надеть «Оберег жизни» (+12 к здоровью). Максимум становится 46.
3. Клавишей `d` (в схеме WASD — `r`) бросить оберег, не снимая.
4. Открыть характеристики.

**Фактически:** слот оберега пуст (`amulet = -1`, это верно), но `max_hp` остался 46. Герой до
конца партии лечится на 12 выше настоящего потолка.
**Ожидалось:** максимум возвращается к 34 — ровно так, как при снятии оберега через котомку.

### Причина

Производные максимумы — здоровье от «Оберега жизни», силы от посоха ведуна и мантии —
хранятся на герое и пересчитываются только в `act_equip` и `check_level_up`. `act_drop`
вызывает `Inventory::take`, который очищает слот, если брошенный предмет был надет, но
`derived_max_hp()` и `derived_max_mana()` не трогает.

Остальные характеристики — удар, защита, обзор, скорость — считаются на лету от текущей
экипировки при каждом обращении и потому самокорректируются. `max_hp` и `max_mana` — нет.

### Правка

После `take` в `act_drop` пересчитываются оба максимума и подрезаются текущие значения — те же
четыре строки, что уже стоят в `act_equip`:

```cpp
hero_.a.max_hp = derived_max_hp();
hero_.a.hp = std::min(hero_.a.hp, hero_.a.max_hp);
hero_.max_mana = derived_max_mana();
hero_.mana = std::min(hero_.mana, hero_.max_mana);
```

`act_drop` — единственный путь, где надетый предмет уходил без пересчёта: потребляемое идёт
через `act_use_item`, снаряжение — через `act_equip`, и оба уже пересчитывают.

### Регрессионный тест

`Gear.DroppingAWornCharmOfLifeFoldsItsBonusBackOut` — надеть оберег через `perform(EquipItem)`,
убедиться, что максимум вырос на 12, бросить через `perform(DropItem)` и проверить, что
максимум вернулся к исходному, а текущее здоровье не осталось выше него.

### Чему научило

Производную величину либо считают на лету при каждом обращении, либо пересчитывают на каждом
пути, который на неё влияет. Второй список легко недосчитать: «бросить» оказался третьим
после «надеть» и «снять».


---

## NAV-020

**Заморозка героя ничего не делала: Морозко «сковывает», а ноги идут**

**Серьёзность:** значительная (баланс) — механика мини-стража не работает, а строка в
журнале и комментарий в коде обещают обратное.
**Найдено:** аудитом кода при подготовке нового эффекта «сон» — искал, где движок вообще
запрещает герою ход, и не нашёл.

### Шаги воспроизведения

1. Спуститься к Морозко (глубина 11).
2. Дать ему сковать героя: не разрывать линию взгляда на его телеграфе «Тепло ли тебе?».
3. В журнале: «Стужа хватает — ноги не идут.»
4. Нажать направление движения.

**Фактически:** герой спокойно идёт. `Effect::Freeze` на герое не проверяется нигде.
**Ожидалось:** пока `Freeze` активен, ход героя теряется — ровно как обещает и текст, и
комментарий рядом с кодом Морозко («Standing still is the mistake; the freeze is short
enough to be survivable and long enough to be frightening»).

### Причина

`Actor::can_act()` — это `alive && !has(Effect::Freeze)`, и читается как общее правило для
всех. Но вызывается он в одном месте: в планировщике, в цикле ходов монстров
(`if (m.a.can_act()) monster_turn(i);`). Путь героя (`Game::perform` → `act_move` и прочее)
его не смотрит. Заморозка задумывалась двусторонней, а подключена была только к одной стороне.

### Правка

В начале `Game::perform`, если герой под `Freeze`, действие фронтенда отбрасывается, и вместо
него проходит `Wait`. Один общий шлюз — на шаг, бег и зажатую клавишу сразу; тот же шлюз
следующим коммитом принимает новый `Effect::Sleep` Кота Баюна.

### Регрессионный тест

`Ai.AFrozenHeroLosesTheTurn` — три попытки шага под трёхходовой заморозкой не сдвигают героя,
а первый шаг после неё — сдвигает.

### Чему научило

Симметричное правило («и монстр, и герой») надо проверять с обеих сторон и подключать с
обеих. `can_act()` выглядел общим, но место вызова было одно — и вторая сторона тихо
осталась ни при чём.


---

## NAV-021

**Обращение к освобождённой памяти в `monster_summon` после призыва**

**Серьёзность:** критическая — чтение и запись по висячей ссылке. Санитайзер валит партию
сразу; без санитайзера это тихая порча кучи, которая проявляется когда угодно и чем угодно.
**Найдено:** обзорным прогоном под ASan + UBSan (`./build-san/nav --sweep`) после набора
Этапа 5. Правки Этапа 5 сдвинули поток ГПСЧ ровно настолько, что призыв стал случаться при
`monsters.size() == capacity` — и латентный дефект наконец выстрелил.

### Шаги воспроизведения

1. Сборка с `-fsanitize=address,undefined`.
2. `./build-san/nav --sweep 1` (сид 1374552062).
3. Партия доходит до боя, где призыватель (`колдун`, `Кощей` в третьей фазе, `Баба-Яга`)
   зовёт подмогу в момент, когда вектор монстров этажа заполнен под завязку.

**Фактически:** `AddressSanitizer: heap-use-after-free ai.cpp:751 in
nav::Game::monster_summon(nav::Monster&)`, запись размером 4 (`m.summon_cooldown = …`).
Память освобождена в `spawn_species` → `std::vector<Monster>::push_back` → реаллокация.
**Ожидалось:** призыв ничего не ломает независимо от текущей ёмкости вектора.

### Причина

`monster_summon(Monster& m)` держит ссылку на призывателя — элемент
`level().monsters`. `spawn_species` делает `push_back` в этот же вектор; когда
`size() == capacity`, вектор переезжает, и `m` повисает. Дальше по коду шли
`m.summon_cooldown = …` и `monster_name(m)` — запись и чтение по освобождённому адресу.

Тот же класс — ещё в двух местах, обнажённых тем же сдвигом ГПСЧ:

* `update_boss_phase(Monster& m)` вызывается из `damage_monster`, которая сама держит
  `Monster& m`; фаза-3 Бабы-Яги поднимала там избушку через `spawn_species` — реаллокация
  из-под вызывающего.
* Ветка третьей фазы Кощея писала `m.summon_cooldown = 4` уже **после** `monster_summon(m)`.

Дефект был с самого добавления призыва; до Этапа 5 распределение по потоку ГПСЧ просто ни
разу не поставило призыв на границу ёмкости за весь прогон.

### Правка

* В `monster_summon` всё, что читается из `m`, снимается в локальные переменные **до**
  первого `spawn_species`: `wanted`, `origin = m.a.pos`, `who = monster_name(m)`,
  `seen = map().visible(origin)`, и `m.summon_cooldown` выставляется там же. Цикл призыва и
  сообщение работают только с локальными копиями.
* Подъём избушки Бабы-Яги перенесён из `update_boss_phase` в `boss_turn` — на её
  собственный ход, где никто не держит внешнюю ссылку на неё сквозь вызов. Разовость
  закреплена через `m.revives` (у неё больше ничем не занят): `phase >= 3 && revives == 0`.
* Из ветки третьей фазы Кощея убран `m.summon_cooldown = 4` после `monster_summon(m)` —
  кулдаун и так выставляется внутри `monster_summon`.

### Регрессионный тест

`Ai.ASummonerSurvivesItsOwnReinforcements` — оставить `колдуна` рядом с бессмертным героем
на 240 ходов, чтобы вектор монстров прошёл через несколько удвоений ёмкости прямо во время
призывов; под ASan прежний код падал на первом же. Проверяется, что призыватель по-прежнему
в списке, что реаллокация действительно случилась (монстров больше трёх) и что все они на
карте. `Phases.BabaYagaRaisesAFreshHutWhenSheIsNearlyDone` дополнен: изба появляется на
следующий ход Яги, а не в момент удара, и ровно одна.

### Чему научило

Функция, которая принимает `T&` на элемент вектора и где-то внутри может тот же вектор
`push_back`-нуть, — это дефект, ждущий своей ёмкости. Снимать все нужные поля в локальные
до первого добавления; спавн, меняющий контейнер, держать подальше от кода, который прошёл
внутрь по ссылке на элемент этого контейнера. И обзорный прогон под санитайзерами ловит
ровно тот класс, что «работает» ровно до сдвига в потоке ГПСЧ.
